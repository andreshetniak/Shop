@model HomeViewModel

<h1>Best cars</h1>
<div class="row mt-5 mb-2">
    @{ 
        foreach (Car car in Model.FavoriteCars)
        {
            @Html.Partial("AllCars", car);
        }
    }
</div>